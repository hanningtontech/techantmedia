import PortfolioAdminPage from "./PortfolioAdminPage";

/** Dedicated entry point — same storefront admin, opens xAI portfolio section. */
export default function XaiPortfolioAdminPage() {
  return <PortfolioAdminPage initialSection="xai.header" />;
}
